const manageCourtsContainerId = "#ManageCourtsContainer";
const manageCourtContainerId = "#ManageCourtContainer";
const alertContainerId = "AlertContainer";

$(document).ready(() => {
    renderCourts();
});

